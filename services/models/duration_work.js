/*const Sequelize = require('sequelize')
const sequelize = require('../utils/database')

const durationWork = sequelize.define('duration_work', {
  dw_id: {
    type: Sequelize.INTEGER(2),
    primaryKey: true,
    autoIncrement: true,
    allowNull: false
  },
  dw_time: {
    type: Sequelize.INTEGER(5),
    allowNull: false
  }
})*/
/*
durationWork.bulkCreate([
  {dw_time: 60},
  {dw_time: 20}
])
*/
/*module.exports = durationWork*/